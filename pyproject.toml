[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ragforge"
version = "0.1.0"
description = "End-to-end LLM, RAG and Agentic AI platform"
requires-python = ">=3.11,<3.13"

[tool.setuptools.packages.find]
where = ["src"]